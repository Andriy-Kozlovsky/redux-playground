import {createTheme} from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#212121",
    },
    secondary: {
      main: "#26C6DA",
    },
    info: {
      main: "#424242",
    },
    background: {
      default: "#424242",
      paper: "#eeeeee",
    },
  },
});

export default theme;
